services:
  # 1. FastAPI Web Service (Backend)
  - type: web
    name: portfolio-backend
    runtime: python
    region: ohio
    plan: free
    buildCommand: pip install -r backend/requirements.txt
    startCommand: uvicorn backend.app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: OPENROUTER_API_KEY
        sync: false
      - key: PINECONE_API_KEY
        sync: false
      - key: PINECONE_ENVIRONMENT
        sync: false
      - key: PINECONE_INDEX_NAME
        sync: false
      - key: ALLOWED_ORIGINS
        sync: false

  # 2. React Static Site (Frontend Widget)
  - type: static_site
    name: portfolio-widget
    runtime: node
    region: ohio
    plan: free
    buildCommand: cd widget/frontend && npm.cmd install && npm.cmd run build
    publishPath: widget/dist
    headers:
      - path: /*
        name: Access-Control-Allow-Origin
        value: "*"
